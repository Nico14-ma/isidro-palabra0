import React from "react";
import ReactDOM from "react-dom/client";
import TracingPalabrasApp from "./TracingPalabrasApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TracingPalabrasApp />
  </React.StrictMode>
);